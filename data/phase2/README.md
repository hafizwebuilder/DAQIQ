Phase 2 placeholder directory.

Expected handoff artifacts for full-city integration:

- updated summary JSON
- updated full-city `gold_webgis_layer.geojson`
- tier-stability sensitivity table artifact
- `gold_ahp_vs_shap_comparison_phase2.csv`

This frontend is already structured so the Phase 1 file paths can be replaced directly with the Phase 2 GeoJSON and summary files. No database or tile service is required at the updated scale.
